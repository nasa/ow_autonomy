#include "plan-interface.h"

Stow:
{
  SynchronousCommand unstow() Timeout 2.0, 0.1;
  SynchronousCommand publish_trajectory() Timeout 5.0, 0.1;
  SynchronousCommand stow() Timeout 2.0, 0.1;
  SynchronousCommand publish_trajectory() Timeout 5.0, 0.1;
}
